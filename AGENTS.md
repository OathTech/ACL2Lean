# AGENTS.md — pointer only

**The working rules for this repository live in [`CLAUDE.md`](CLAUDE.md).**
Read that file; it is binding. This file exists only so agents that look for
`AGENTS.md` by convention are redirected there rather than reasoning from a
stale template.

Also canonical:

- [`docs/LEXICON.md`](docs/LEXICON.md) — the vocabulary (*replayed statement*
  / *waypoint* / *mirror* name three different things; where a dated doc
  conflicts, the lexicon wins).
- [`docs/plans/2026-08-12_master-plan.md`](docs/plans/2026-08-12_master-plan.md)
  — the governing plan.
- [`docs/OVERVIEW.md`](docs/OVERVIEW.md) — the pipeline, the trust model,
  build commands (this was `README.md` until 2026-08-19; the root
  `README.md` is now a short front page).
- [`TODO.md`](TODO.md) — the running backlog.

Note for anyone restoring boilerplate here: the previous contents of this file
were an unmodified Lake starter template describing a different project, and
its advice to "rely on sorry placeholders" directly contradicts CLAUDE.md's
fidelity rules. Do not reintroduce generic guidance; point at CLAUDE.md.
