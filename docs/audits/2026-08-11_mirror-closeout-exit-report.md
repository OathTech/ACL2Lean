# Mirror-closeout arc — exit report (2026-08-11)

Branch `mdd/thin-lean-boundary` (main @ 90412ff → HEAD). Charter:
`docs/plans/2026-08-11_mirror-closeout-charter.md`. Exit audit: two
reviewers (claims = adversarial; gates = deletion review under the
two-standard rule) + this fix round; final claim-gate artifact
recorded in the claim-point commit.

## Scoreboard

- Corpus 110/116 (45 + 65) — UNCHANGED by design (this arc was
  mirror-side; the replay unlocks are the next arcs). Sorting rows 74.
- Catalog natives 44 → **46** (24 native + 22 nativeSorried); sorting
  `.pending` 8 → **6**, every remaining one carrying a named frontier.
- Hand lines per native 148 → **142**; the `_enc` iso layer is 100%
  generated (14/14 regenerated byte-identical, zero hand isos remain).
- Gate code in Catalog.lean 476 → **348** lines (the deletion review);
  count floors 3 → 0; enumerated name registries 5 → 2.
- 18 FORBIDDEN-DEBT sorries — exactly the registered set, unchanged.

## Charter items

1. **Opening ruled increments — DONE** (totsNames deleted;
   hreplayed-usage check; shape gate built THEN demoted same-day by
   the gate-cruft review — see below; F6/P8 under the axiom scan;
   `just mirror-metrics`).
2. **`derive_sim%` — DONE.** One template (a mechanical merge of
   T1/T2: induct on the native's own recursion), validation by
   retirement (14/14, statements + axiom sets byte-identical,
   narrow sets not widened), template-failure-as-gate live. KNOWN
   BOUND (recorded, will not be hardened): discriminating power is
   the fixed closer set — default-simp reassociations pass; the
   ruled accumulator class fails closed.
3. **Sorting `.pending` settlement — DONE** (2 natives, 6 honest
   pendings, 1 corrected stale justification; zero new dischargers,
   zero new support, zero hand isos). The claims auditor verified
   both new natives FAITHFUL against the logs' `:FORMULA` (by-hand
   case analysis incl. the Int→Nat coercion — no truncation hole;
   formulas defeq-pinned to the root Goal clauses) with one accepted
   scope note: natives quantify over the enc image (the
   type-absorbed doctrine), now stated in their docstrings.
4. **Demo — DESIGN DELIVERED** (`docs/plans/2026-08-11_demo-design.md`,
   rulings R1–R4 sought below); the build is charter-gated on the
   ruling and was not started.
5. **Exit audit — DONE** (this report; fix round applied).

## Audit findings and dispositions

- **F1 (DEFECT, FIXED):** the kit registry's import-order inversion —
  downstream modules saw stale (pre-iso) kit copies; `derive_sim%`
  emitted a FALSE misalignment verdict downstream and the
  duplicate-iso guards were defeated cross-file. Zero shipped-proof
  blast radius (all callee isos were same-module); fixed
  (reverse-flatten), probe-verified in both directions, docstring
  claims corrected.
- **Gate-cruft deletion review (two-standard rule):** shape gate
  demoted to the printed census (`Tests/MirrorCensus.lean` — the
  support/other number (47) equals exactly the old registries:
  nothing was hiding behind the floors); hreplayed floor deleted
  (+ the EvTrue tripwire: delete the gate when the predicate stops
  matching, never extend it); axiom gate WIDENED to every Mirrors
  `_driver` theorem (0 unexpected axioms among 56 — strictly
  stronger, subsumes two fail-open hand-lists and the extra-natives
  gate; RECORDED TRADE: 16 names drop from exact-tier pins to
  ⊆ trio+sorryAx); seam BFS deduped; equisort receipts moved from
  text pins to the axiom scan (AtCanonical sorryAx REQUIRED,
  promotion-forcing). KEEP verdicts on everything load-bearing and
  all 13 scripts.
- **Record corrections:** commit 808a7ad's "DIFF EMPTY" was true of
  the info-payload lines but the cited capture files differ in
  filtering (the before-capture was filtered; not reproducible) —
  the substantive equivalence was independently re-verified live
  (3/14 re-derived; capture states corroborated against git).
  Job-count figures in fast-gate commit messages are unfalsifiable —
  future claim points quote the gate artifact's numbers. "Zero hand
  `_enc`" means zero hand `<fn>Exec_enc` iso templates (support
  `_enc`-suffixed lemmas like `trueListp_enc` are a different class
  and remain).
- **Verified-clean:** all six pending frontiers match the golden
  verbatim (the discharger-absence claims checked against the full
  21-constant dis_/drv_ inventory); the PCE frontier reproduced from
  scratch; the CONVERT-PERM correction genuine; quantitative claims
  in commit messages all recounted true; the metrics script extended
  honestly (new modules counted, not hidden).

## Rulings sought (Mike)

1. **Demo design R1–R4** (`docs/plans/2026-08-11_demo-design.md`):
   the Sims/Iso/Decode/Debt split with imports-as-trust-boundary,
   the check-trust-imports ci script, the Showcase with on-page
   receipts, DEMO.md. Note: the gate-cruft review's refactor walk
   found the split's former shape-gate tax is GONE after the
   demotion — the split now costs one baseline line + import wiring.
2. **Reuse-vs-mint (F6 — decides the bsort four):** the wave held a
   conservative "never mint new dischargers" line, but that boundary
   is ARC DISCIPLINE, not the ruling (the boundary note explicitly
   allows visible FORBIDDEN-DEBT sorries). If minting sorried debt
   entries for `tp:HOW-MANY-SMALLER`/`tp:BNEXT-SIZE`/`total:BSORT`/
   `total:O-P`/`linear:HOW-MANY-BAD-PAIRS-BNEXT` is allowed, the
   bsort four land as `.nativeSorried` (+ a P2-legal bsortExec kit);
   if not, they wait for the TP-replay/with_termination arcs.
3. **`derive_sim%` ELEMENT reading** (unlocks the PCE native): the
   template already PROVES the pceL iso; only the reading-table
   frontier blocks it. A ruled generator increment (extend the table
   to element-valued readings) vs leave pending.
4. **The witness-free PCE alternative** (`∃ e, …count e ≠ …count e`
   shape) — a new native shape with no catalog precedent; flagged by
   the wave, built by nobody.

## Carried forward

- The demo build (post-ruling); the R-lane arc (PERM-TLFIX → the
  CONVERT-PERM chain + dis_convert_perm retirement); the TP-replay
  discharge route (the census's top lever: tp: 195); with_termination
  admission coverage (REQUIRED debt); the per-book-family provenance
  audit cadence; the G2 EvTrue-migration tripwire on the usage gate.
